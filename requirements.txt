# --- Core runtime ---
python-telegram-bot==21.6
httpx==0.27.2
feedparser==6.0.11
pydantic==2.9.2
pydantic-settings==2.5.2
python-dotenv==1.0.1
tenacity==9.0.0
structlog==24.4.0
orjson==3.10.7
python-dateutil==2.9.0.post0

# --- Database ---
SQLAlchemy==2.0.36
asyncpg==0.30.0
# SQLAlchemy async needs greenlet at runtime; on Python 3.13 it's no
# longer pulled in automatically via the ``asyncio`` extra.
greenlet==3.1.1

# --- Polymarket / Polygon ---
# CLOB V2 (Apr 2026+): legacy py-clob-client (V1) is rejected with
# order_version_mismatch on production — use the official V2 SDK.
py-clob-client-v2==1.0.0
web3==7.4.0
eth-account==0.13.4

# --- Crypto Mode (Binance + Coinbase WebSockets) ---
# Lightweight raw WS client; we deliberately avoid ccxt.pro to keep the
# dependency footprint small.  ``httpx`` (already pinned above) is used
# for Binance REST candle fetches.
websockets==13.1

# --- Testing ---
pytest==8.3.3
pytest-asyncio==0.24.0
respx==0.21.1
